namespace MediSearch.Core.Application.Companies.Constants;

internal static class CompanyPrefixes
{
    public const string CompanyOwner = nameof(CompanyOwner);
    public const string Company = nameof(Company);
}
